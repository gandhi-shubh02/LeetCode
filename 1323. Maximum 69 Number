class Solution:
    def maximum69Number (self, num: int) -> int:
        l=[int(x) for x in str(num)]
        for i in range(len(l)):
            if l[i]==6:
                l[i]=9
                break
        s=[str(i) for i in l]
        res = int("".join(s))
        return res
            
